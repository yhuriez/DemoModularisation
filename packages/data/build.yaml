# Configuration de build_runner pour le package data
targets:
  $default:
    builders:
      injectable_generator:injectable_builder:
        enabled: true
        options:
          auto_register: true
      freezed:
        generate_for:
          - lib/src/models/*.dart
        options:
          union_key: type
          union_value_case: pascal

